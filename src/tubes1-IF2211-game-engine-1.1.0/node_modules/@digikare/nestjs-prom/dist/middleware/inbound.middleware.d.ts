import { NestMiddleware } from "@nestjs/common";
import { Counter, Histogram } from "prom-client";
import { InboundMiddlewareOptions } from "./inbound.middleware-options";
export declare class InboundMiddleware implements NestMiddleware {
    private readonly _options;
    private readonly _requestsTotal;
    private readonly _requestsDuration;
    constructor(_options: InboundMiddlewareOptions, _requestsTotal: Counter<string>, _requestsDuration: Histogram<string>);
    use(req: any, res: any, next: any): void;
}
